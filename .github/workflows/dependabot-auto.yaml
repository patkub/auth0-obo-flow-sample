# Auto update code on Dependabot PRs
#   - Migrates Biome configuration to reference latest schema
#   - Tests Auth0 OBO Flow Action

name: Update on Dependabot PR
on:
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  generate-code:
    # Only run this job for Dependabot PRs
    if: ${{ github.actor == 'dependabot[bot]' }}
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./auth0-tenant
    permissions:
      # Give the default GITHUB_TOKEN write permission
      # to commit and push the changed files back to the repository.
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: ${{ github.head_ref }}
          # Value already defaults to true, but `persist-credentials` is required
          # to push new commits to the repository.
          persist-credentials: true

      - name: Use Node.js 22.x
        uses: actions/setup-node@v7
        with:
          node-version: 22.x
          cache: "npm"
          cache-dependency-path: './auth0-tenant/package-lock.json'

      - name: Install dependencies
        run: npm install

      # Biome migrate
      - name: Biome migrate
        run: npx biome migrate --write

      # Test Auth0 OBO Flow
      - name: Test Auth0 OBO Flow
        uses: ./.github/actions/test-obo-flow
      
      # Commit all changed files back to the repository
      - name: Commit and push changes
        # This action handles committing any changes made by the script
        uses: stefanzweifel/git-auto-commit-action@v7
        with:
          # Add [dependabot-skip] to the commit message to prevent Dependabot
          # from treating the PR as manually modified and stopping updates.
          # Must use dependabot-skip or skip-dependabot so that global GitHub
          # Action skip is NOT triggered.
          commit_message: "chore(deps): [dependabot-skip] auto updates"

{
  "name": "auth0-tenant",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "deploy": "a0deploy import -c auth0-config.json -i auth0-tenant",
    "export": "a0deploy export -c auth0-config.json -o exported-tenant -f directory",
    "lint": "biome lint --write --unsafe",
    "lint:check": "biome lint",
    "format": "biome format --write",
    "format:check": "biome format",
    "test": "vitest run --silent --coverage",
    "test:watch": "vitest --silent"
  },
  "dependencies": {
    "auth0-deploy-cli": "^9.0.0"
  },
  "devDependencies": {
    "@auth0/actions": "^0.34.0",
    "@biomejs/biome": "^2.5.14",
    "@vitest/coverage-v8": "^5.0.1",
    "vitest": "^5.0.1"
  }
}
